# Erply NEWS API test task

### Overview 
Project task for Erply

### Development env
Pull the repo, run `npm install` and then `npm start` in the root of the project. Files will be served at `localhost:3000`.